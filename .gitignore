# Build output
target/

# Runtime-generated, user-writable state (bootstrapped from bundled
# src/main/resources defaults on first run -- see RuleRepository/SimulationConfig)
config/

# Tool-local / agent config (not project source)

# IDE metadata
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS / editor temp files
.DS_Store
*~
*.tmp
*.swp
